export interface Shelter {
  id: number;
  name: string;
  district: string;
  capacity: number;
  current_population: string;
  contact_person: string;
  risk_level: string;
  open_status: string;
}
